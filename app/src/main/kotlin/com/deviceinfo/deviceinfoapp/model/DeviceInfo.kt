package com.deviceinfo.deviceinfoapp.model

/**
 * A simple data class to hold a piece of device information.
 * @param label The name of the information (e.g., "Device Model").
 * @param value The value of the information (e.g., "Pixel 6 Pro").
 */
data class DeviceInfo(
    val label: String,
    val value: String
)```
